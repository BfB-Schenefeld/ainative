# Quiz items — AI-Native SAFe Overview

30 single-choice items, the complete final-quiz bank. Every item carries the confirmed
correct answer and the app's own feedback text for **every** option, right and wrong —
the wrong-answer feedback is often the clearest statement of the underlying concept in
the whole corpus.

Mechanics, from the same source: the quiz draws **15 at random from these 30**, passing
score is **12 (80%)**, open book, unlimited retries.

Source: `src:2026-08-26/upgrade-path-final-quiz`. See that source record for the
provenance caveat — the originating bundle chunk was redeployed away before it could be
archived byte-for-byte.

One file per item, `NNN.yaml`, validated against `schemas/quiz-item.schema.json`.
