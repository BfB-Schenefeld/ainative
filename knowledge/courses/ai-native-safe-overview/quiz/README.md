# Quiz items — AI-Native SAFe Overview

**Empty on purpose.** The 2026-08-25 capture contains no quiz content: the Final Quiz is
listed in the lesson menu, but Rise 360 holds question banks in a JavaScript payload
that SingleFile strips. Nothing was inferred or invented.

One file per item, `NNN.yaml`, validated against `schemas/quiz-item.schema.json`.
Set `answer_status: unknown` unless the answer key was actually captured.
